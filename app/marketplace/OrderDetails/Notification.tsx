const Notification = () => {
    return (
        <div className="notification-box mt-3">
            <i className="fas fa-bell"></i>
            <p>You’ll be notified via SMS, email, app inbox, and push notifications when your package is ready for pickup.</p>
        </div>
    );
};

export default Notification;